# Copy to .env.local and customise for your local environment
# Use separate DBs/ports if running multiple projects (e.g., CapOpt)

# Connection string for PostgreSQL MCP server
POSTGRES_MCP_CONNECTION_STRING="postgresql://cw_dev_mcp:changeme@localhost:5432/councilworks_dev"
